# remove this:
exit 0;


@names = ({
	# zone/domain to use
	domain => 'dyn.example.com',

	# the hostname within this zone to update:
	name => 'sample',

	# key as generated by dnssec-keygen  -a HMAC-MD5 -b 512 -n USER <name>
	key  => "fadfaf<shortened>asfasfaffad==",
});

1;
